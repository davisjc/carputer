#!/bin/sh

_SCRIPT_DIR="$(cd "$(dirname "${BASH_SOURCE[0]}")" && pwd)"
_MUSIC_DIR=${HOME}/music
_TRANSPORT_DIR=/mnt/carputer_transport
_TRANSPORT_ID=$(cat "${_SCRIPT_DIR}/id")
_LIST_PC=${_TRANSPORT_DIR}/"file_list_pc.txt"
_LIST_CAR=${_TRANSPORT_DIR}/"file_list_car.txt"

